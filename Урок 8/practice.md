# 💪 Практическое занятие — Повторение

> Решай задачи по порядку. Не торопись — лучше сделать меньше, но правильно!

---

## Блок 1 — Переменные и типы

**Задача 1**

Создай четыре переменные: своё имя, возраст, рост (с десятичной точкой) и `is_student = True`. Выведи все четыре в одной f-строке.

```
Меня зовут Арстан, мне 22 года, мой рост 1.75м, студент: True
```

---

**Задача 2**

Попроси пользователя ввести своё имя через `input()`. Выведи его заглавными буквами и посчитай сколько в нём символов.

```
Введите имя: арстан
АРСТАН — 6 символов
```

---

## Блок 2 — Условия и циклы

**Задача 3**

Пользователь вводит число. Выведи `"Чётное"` или `"Нечётное"`.

```
Введите число: 7
Нечётное
```

---

**Задача 4**

Напиши цикл который выводит числа от 1 до 20, но вместо чисел кратных 3 выводит `"Fizz"`.

```
1
2
Fizz
4
5
Fizz
...
```

---

**Задача 5**

Пользователь вводит пароль. Дай ему 3 попытки — если угадал, выведи `"Добро пожаловать!"`. Если все три попытки неверны — выведи `"Аккаунт заблокирован"`.

```
Введите пароль: abc
Неверно, осталось 2 попытки
Введите пароль: 123
Неверно, осталось 1 попытки
Введите пароль: qwerty
Добро пожаловать!
```

---

## Блок 3 — Функции

**Задача 6**

Напиши функцию `is_even(number: int) -> bool` которая возвращает `True` если число чётное, и `False` если нечётное.

```python
print(is_even(4))   # True
print(is_even(7))   # False
```

---

**Задача 7**

Напиши функцию `clamp(value: int, min_val: int, max_val: int) -> int` которая возвращает `value`, но не меньше `min_val` и не больше `max_val`.

```python
print(clamp(15, 0, 10))   # 10
print(clamp(-5, 0, 10))   # 0
print(clamp(7, 0, 10))    # 7
```

---

## Блок 4 — Коллекции

**Задача 8**

Дан список оценок. Выведи среднее, максимум и минимум.

```python
grades = [85, 42, 91, 58, 73, 66]
```

```
Среднее: 69.17
Максимум: 91
Минимум: 42
```

---

**Задача 9**

Дан словарь с именами и возрастами. Используй **генератор словаря** чтобы создать новый словарь только с теми у кого возраст больше 18.

```python
people = {"арстан": 22, "алия": 21, "жантай": 16}
```

```
{"арстан": 22, "алия": 21}
```

---

**Задача 10**

Дан список слов. Используй **генератор списка** чтобы получить только слова длиннее 4 символов, заглавными буквами.

```python
words = ["привет", "мир", "python", "код", "класс"]
```

```
["ПРИВЕТ", "PYTHON", "КЛАСС"]
```

---

## Блок 5 — Обработка ошибок

**Задача 11**

Напиши функцию `safe_divide(a, b)` которая делит `a` на `b`. Обработай деление на ноль и некорректный тип данных.

```python
safe_divide(10, 2)    # 5.0
safe_divide(10, 0)    # "Ошибка: деление на ноль"
safe_divide(10, "a")  # "Ошибка: некорректный тип данных"
```

---

**Задача 12**

Напиши программу которая читает файл `notes.txt` и выводит его содержимое строка за строкой. Если файла не существует — выводит `"Файл не найден"`.

---

## Блок 6 — Классы

**Задача 13**

Создай класс `Rectangle` с атрибутами `width` и `height`. Добавь методы:

- `area() -> float` — возвращает площадь
- `perimeter() -> float` — возвращает периметр
- `__str__` — возвращает:

```
Прямоугольник 4x6 | Площадь: 24 | Периметр: 20
```

---

**Задача 14**

Создай дочерний класс `Square(Rectangle)`. Конструктор принимает только одну сторону `side` — используй `super()`. Убедись что `area()` и `perimeter()` работают без изменений.

```python
sq = Square(5)
print(sq)  # Прямоугольник 5x5 | Площадь: 25 | Периметр: 20
```

---

> 🏁 Если дошёл до сюда — отличная работа! Задачи 1–5 самые базовые, 6–14 требуют больше думать. Не расстраивайся если застрял — спрашивай!
